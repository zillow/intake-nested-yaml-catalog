metadata:
  hierarchical_catalog: True
level1:
  tables6:
    args:
        urlpath: "{{ CATALOG_DIR }}/files*"
    description: "incompatible plugins"
    driver:
      myplug:
          class: csv
      myplug2:
          class: numpy
    metadata: {}
